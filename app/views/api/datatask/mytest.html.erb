# create pack "<%= @packName %>" <br />
pack<%= @packName %>ID = "<%= @packID %>" <br />
     <br />
createPack(pack<%= @packName %>ID, "<%= @packName %>", "<%= @packImage %>") <br />
     <br />
<% @commands.each do |a| %>
<%= a %> <br />
<% end %>